#version: "3.8"
  services:
    worker:
     build: .
     environment:
      BOT_TOKEN: $BOT_TOKEN
      API_ID: $API_ID
      API_HASH: $API_HASH
      CHANNELS: $CHANNELS
      ADMINS: $ADMINS
      LOG_CHANNEL: $LOG_CHANNEL
      DATABASE_NAME: $DATABASE_NAME
      DATABASE_URI: $DATABASE_URI
      AUTH_CHANNEL: $AUTH_CHANNEL
      PICS: $PICS
      FILDLT_CNL: $FILDLT_CNL 
      AUTH_GROUPS: $AUTH_GROUPS
      COLLECTION_NAME: $COLLECTION_NAME
      RQST_LOG_CHANNEL: $RQST_LOG_CHANNEL 
      FILE_CHANNEL_LINK: $FILE_CHANNEL_LINK
      FILE_CHANNEL: $FILE_CHANNEL
      CACHE_TIME: $CACHE_TIME
      SPL_DELETE_TIME: $SPL_DELETE_TIME
      DELETE_TIME: $DELETE_TIME
      AUTO_FFILTER: $AUTO_FFILTER
